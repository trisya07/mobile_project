import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionict.starter',
  appName: 'Share Lok',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
